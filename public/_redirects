# Simplified for debugging Worker threw exception
# # 多语种子域名重定向规则
# # 主域名重定向到英文子域名
# # Removed potentially problematic redirect rules that might cause a loop
#
# # 各语种子域名的SPA路由支持
# /es/* /index.html 200
# /ja/* /index.html 200
# /de/* /index.html 200
# /fr/* /index.html 200
# /ru/* /index.html 200
# /th/* /index.html 200
# /uk/* /index.html 200

# 默认SPA路由支持（用于开发和其他情况）
/*  /index.html  200